{
  "name": "coursework-mcp-servers",
  "private": true,
  "version": "1.0.0",
  "description": "Portable MCP server bundle: filesystem scoped to a coursework folder, plus a zero-dependency web search server.",
  "dependencies": {
    "@modelcontextprotocol/server-filesystem": "2025.8.21"
  },
  "overrides": {
    "zod": "3.25.76"
  }
}
